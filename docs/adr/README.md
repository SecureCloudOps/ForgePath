# Architecture Decision Records

ForgePath will use short architecture decision records (ADRs) for decisions that
materially affect security, delivery, or project scope.

No decisions are recorded yet. Add ADRs only when an implementation decision is
required, using sequential filenames such as `0001-short-title.md`.
